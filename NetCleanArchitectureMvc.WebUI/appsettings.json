{
  "ConnectionStrings": {
    "DefaultConnection": "Data Source=ARTVAC101\\SQLEXPRESS; Initial Catalog=CleanArchDB;Integrated Security=True ;TrustServerCertificate=True"

  },

  /*
    "Data Source=ARTVAC101\SQLEXPRESS": Isso especifica o nome do servidor SQL Server como "ARTVAC101" e a instância do SQL Server como "SQLEXPRESS". Verifique se essas informações estão corretas e correspondem à configuração do seu servidor SQL Server.

    "Initial Catalog=Teste": Isso especifica o nome do banco de dados como "Teste". Verifique se esse banco de dados já existe no servidor SQL Server ou se você precisa criar um novo banco de dados com esse nome.

    "Integrated Security=True": Isso indica que a autenticação integrada do Windows será usada para se conectar ao banco de dados. Isso significa que as credenciais de autenticação do usuário atualmente logado no sistema serão usadas para a autenticação. Verifique se o usuário que está executando o aplicativo tem as permissões adequadas para acessar o banco de dados.

  */

  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*"
}
